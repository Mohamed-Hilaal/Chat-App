/*import React,{useContext, useState} from 'react'
import { ContactsProvider } from './components/context/ContactsProvider'
import {ListGroup,Button, FormGroup, Form, Navbar, NavItem,Nav} from "react-bootstrap"

function Practice(){

  const [Product,setProduct] = useState("Hey There")


function HandleSubmit(){
  let madeChange = false
  
  if(madeChange ){
 
    setProduct("No You are Not there")
    console.log(madeChange)
  }
     else{
      madeChange = true
       setProduct("Else Works")
       console.log(madeChange)
     }

}




  return(
   <div>
<div>
  {Product}
</div>
       <Button onClick={HandleSubmit}>
         Submit
       </Button>
   </div> 
  )
}

export default Practice
<ContactsProvider.Provider value={value}>
  { id?<Dashboard id={id}/>:<Login onIdSubmit={setId}/>}
   
      </ContactsProvider.Provider>*/


      import React from 'react'
      
      function Practice() {
        return (
          <div>
            
          </div>
        )
      }
      
      export default Practice
      